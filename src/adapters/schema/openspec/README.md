# OpenSpec Adapter Placeholder

This directory is reserved for a future `OpenSpec` schema adapter.

Phase 1 and Phase 2 of the orchestration refactor establish the contracts and boundaries first. Actual compatibility work should happen only after the `lee-spec` adapter path is stable and tested.
